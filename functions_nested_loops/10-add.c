#include "main.h"
/**
 * add - prints the add of two integers
 * @num1: number one
 * @num2: number two
 * Return: Add of number one and number two.
 */

int add(int num1, int num2)
{
	int result;

	result = num1 + num2;
	return (result);
}
